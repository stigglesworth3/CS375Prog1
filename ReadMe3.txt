To run program3 enter the following commands:
make program3
./program3 *file name that contains string X* *file name that contains string Y* *file name for what the output should be*

this program uses a matrix (2D array) to store the information that has already been calculated in previous recursions as to avoid repeating recursion

compared to the other 2 programs in this assignment, program1 should run the fastest out of all of them
