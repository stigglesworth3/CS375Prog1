To run program2 enter the following commands:
make program2
./program 2 *file name that contains string X* *file name that contains string Y* *file name for what the output should be*

this program uses no data structures besides simple variables and recursion 

compared to the other 2 programs in this assignment, program2 should run as the second fastest
